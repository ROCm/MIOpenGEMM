21 Nov 2016.

#add_definitions(-DAVARIABLE=123123123123123)
#add_definitions(-DDIR_FOR_WRITING="${CMAKE_CURRENT_BINARY_DIR}/output")
#set(DIR_FOR_WRITING "${CMAKE_CURRENT_BINARY_DIR}/output" CACHE FILEPATH "A directory where tinygemm has permission to write and then remove files")
#set(bla blabla)
#file(MAKE_DIRECTORY "${DIR_FOR_WRITING}")
#file(MAKE_DIRECTORY "${DIR_FOR_WRITING}/isa_code")
#file(MAKE_DIRECTORY "${DIR_FOR_WRITING}/kernels")
#file(MAKE_DIRECTORY "${DIR_FOR_WRITING}/output")
#file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/output")
#file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/output/isa_code")
#file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/output/kernels")
#file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/output/output")





############### On JN's catalyst, set these in build/CMakeCache.txt #################################################################################
#//Path to a file.
#OpenCL_INCLUDE_DIR:PATH=/opt/AMDAPPSDK-3.0/include
#//Path to a library.
#/home/james/opencl/lib/x86_64/libOpenCL.so
#//DO NOT USE THE FOLLOWING PATH (IT HAS AN INFERIOR COMPILER, BUGGERS UP MY 8x8 !!!) : 
#OpenCL_LIBRARY:FILEPATH=/opt/AMDAPPSDK-3.0/lib/x86_64/libOpenCL.so

############### On JN's rocm, set this in build/CMakeCache.txt (otherwise it finds another version which prints out isa code to terminal!!!) #########
#OpenCL_LIBRARY:FILEPATH=/home/jam es/opencl/lib/x86_64/libOpenCL.so
